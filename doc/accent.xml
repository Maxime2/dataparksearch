<sect2 id="accent">
	<title>Accent insensitive search</title>
<indexterm><primary>Accent insensitive search</primary></indexterm>
<indexterm><primary>Command</primary><secondary>AccentExtensions</secondary></indexterm>
	<para>Since version 4.17 <application>DataparkSearch</application> also support an
accent insensitive search.</para>

<para>To enable this extension, use <command>AccentExtensions</command> command in your 
<filename>search.htm</filename> (or in <filename>searchd.conf</filename>, if <command>searchd</command>
is used) to make automatically accent-free copies for query words, and in your <filename>indexer.conf</filename>
config file to produce accent-free word's copies to store in database.
<programlisting>
AccentExtensions yes
</programlisting>
</para>
<para>If <command>AccentExtensions</command> command is placed before <command>Spell</command> and <command>Affix</command> commands,
accent-free copies for those data also will be loaded automaticaly.
</para>
</sect2>

